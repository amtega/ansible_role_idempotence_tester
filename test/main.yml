---
# Tasks for tesing role

- name: load docker presets and configure docker sandbox
  hosts: localhost
  roles:
   - docker_presets
  tasks:
    - name: setup facts to configure docker sandbox
      set_fact:
        docker_sandbox_images: >-
          {{ docker_presets_images }}
        docker_sandbox_containers: >-
          {{ docker_presets_containers | docker_presets_randomize_names }}

- name: start docker sandbox and run idempotence test with ok expect results
  hosts: localhost
  roles:
    - role: docker_sandbox
      docker_sandbox_state: started
      docker_sandbox_idempotence_test_tag: correct_idempotence_test
  tasks:
    - name: assert that idempotence test was ok
      assert:
        that: not idempotence_tester_test_result | failed

- name: run idempotence test with no changes expected result
  hosts: localhost
  tasks:
    - include_role:
        name: docker_sandbox
      vars:
        docker_sandbox_state: restarted
        docker_sandbox_idempotence_test_tag: no_changes_idempotence_test
      ignore_errors: true

    - debug: var=idempotence_tester_test_result

    - name: assert that idempotence test failed
      assert:
        that:
          - idempotence_tester_test_result | failed
          - idempotence_tester_test_result.msg | search('unchanged')

- name: run idempotence test with failed expected result
  hosts: localhost
  tasks:
    - include_role:
        name: docker_sandbox
      vars:
        docker_sandbox_state: absent
        docker_sandbox_idempotence_test_tag: failed_idempotence_test
      ignore_errors: true

    - name: assert that idempotence test failed
      assert:
        that:
          - idempotence_tester_test_result | failed
          - idempotence_tester_test_result.msg | search('failed')

- name: refresh inventory to avoid running tasks destinated to idempotence tests
  hosts: localhost
  tasks:
    - meta: refresh_inventory

- name: idempotence test with correct expected result
  hosts: docker_sandbox_containers
  tasks:
    - name: create an empty file
      copy:
        src: /etc/issue
        dest: /tmp/
        force: no
  tags:
    - correct_idempotence_test

- name: idempotence test with no changes expected result
  hosts: docker_sandbox_containers
  tasks:
    - debug: msg="Hello World"
  tags:
    - no_changes_idempotence_test

- name: idempotence test with failed expected result
  hosts: docker_sandbox_containers
  tasks:
    - name: create an empty file
      copy:
        src: /missingdir/missingfile
        dest: /tmp/
        force: no
  tags:
    - failed_idempotence_test
